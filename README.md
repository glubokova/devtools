![CI](https://github.com/glubokova/devtools/actions/workflows/ci.yml/badge.svg)


# DVT-11

## Информационный поиск — результаты 

### Запросы и источники

| №                                                                | Запрос     | Операторы                                             | Официальный источник                                                | Альтернатива | Статус | Дата проверки |
|------------------------------------------------------------------|------------|-------------------------------------------------------|---------------------------------------------------------------------|--------------|--------|---------------|
| 1    | Lombok Gradle Short | site:search.maven.org"lombok"  | https://mvnrepository.com/artifact/org.projectlombok/lombok/1.18.38 | https://mvnrepository.com/artifact/org.projectlombok/lombok/1.18.30 | 200 OK     | 31.01.2026   |
| 2    | Java Stream API | site:docs.oracle.com "stream api" intitle:api java 25 | https://mvnrepository.com/artifact/org.slf4j/slf4j-api/2.0.17  || 200 OK | 31.01.2026 |
| 3    | keymap intelijIDEA | site:www.jetbrains.com/help "keymap" "intellij idea"  | https://mvnrepository.com/artifact/org.jetbrains/annotations/26.0.2  || 200 OK | 31.01.2026 |

### AI-промпты и сравнение ассистентов

#### Промпт 1: План решения

**Задача: Нужно добавить обработку пустого или null массива в методе calculateTotalProgress класса ProgressTracker из задания DVT-6 **
**Контекст: Java 17, у меня уже есть реализованный метод calculateTotalProgress(Mentee[] mentees),
который работает корректно для непустого массива. Проект на Gradle с тестами JUnit 5 и AssertJ.**
**Ограничения: Нельзя использовать сторонние библиотеки для валидаци, нужно сохранить текущую логику работы цикла while**
**Ожидаемый результат: План из 3-5 шагов с объяснением "почему именно так". НЕ пиши код — я реализую сам.**
**Критерии успеха: - Метод корректно обрабатывает null массив (возвращает понятное сообщение или выбрасывает исключение)
                   - Метод корректно обрабатывает пустой массив (возвращает сообщение о нулевом прогрессе)**

**AI-1 ([DeepSeek]): План решения:
Добавить валидацию входного параметра в первую очередь
Почему: Защитное программирование предотвращает неожиданные сбои
Как: Проверить if (mentees == null) и обработать этот случай
Выбрать подход к обработке ошибочных данных:
Вариант A: Выбросить IllegalArgumentException для null
Вариант B: Вернуть дефолтное значение для пустого/нулевого массива
Почему именно A: Лучше явно указать на ошибку, чем молча возвращать некорректный результат
Обработать edge-case с пустым массивом:
Почему: length == 0 — валидное состояние, но требует особой обработки
Как: Вернуть строку "В группе нет студентов" или аналогичную
Структура метода после изменений:
Шаг 1: Проверка на null → исключение
Шаг 2: Проверка на пустой массив → специальное сообщение
Шаг 3: Существующая логика с циклом while
Рефакторинг с учетом читаемости:
Почему: Добавление проверок не должно ухудшать читаемость
Как: Вынести проверки в отдельные if-блоки в начале метода
Важное замечание: Не смешивать обработку null и пустого массива — это разные семантические случаи.**

**AI-2 ([ChatGPT]): План действий
1️⃣ Определиться с контрактом метода для null

Что сделать:
В самом начале метода явно обработать ситуацию, когда mentees == null.

Почему так:

null — это ошибка использования метода, а не «нормальное состояние данных».

Явная реакция (например, исключение с понятным сообщением) делает ошибку заметной сразу, а не где-то внутри while.

Начинающим разработчикам проще понять: «метод нельзя вызывать с null».

👉 Важно: выбери поведение, которое не ломает существующие тесты. Если раньше null не передавался — ранний throw безопасен.

2️⃣ Отдельно обработать пустой массив

Что сделать:
Сразу после проверки на null проверить mentees.length == 0 и вернуть результат, соответствующий «нулевому прогрессу».

Почему так:

Пустой массив — это валидный вход, в отличие от null.

Логика while для этого случая не нужна и только усложнит чтение.

Явный ранний return делает намерение кода очевидным:
«Если участников нет — прогресс нулевой».

3️⃣ Сохранить существующий while без изменений

Что сделать:
Ничего не менять в текущей логике цикла while, который считает прогресс для непустого массива.

Почему так:

Это ключевое требование задания.

Ранние проверки (null / пусто) изолируют крайние случаи и не влияют на основной алгоритм.

Минимальный риск сломать рабочую логику и существующие тесты.

4️⃣ Зафиксировать поведение тестами (мысленно или реально)

Что сделать:
Проверить (или добавить) два сценария:

null → ожидаемое исключение или сообщение

пустой массив → результат с нулевым прогрессом

Почему так:

Ты явно документируешь контракт метода.

Гарантируешь, что будущие изменения не сломают эту логику.

Существующие тесты продолжают проходить → обратная совместимость сохранена.**

**Сравнение: Общие черты, которые совпадают в обоих планах:
Оба подхода сходятся в главном — последовательность действий одинаковая. Сначала проверяем null,
потом пустой массив, и только затем запускаем основной цикл. И ваш план, и мой чётко разделяют
семантику: null — это ошибка, а пустой массив — валидное, но крайнее состояние. Мы оба настаиваем
на принципе fail-fast (раннее обнаружение проблем) и на том, что существующая рабочая логика цикла while
не должна меняться. Также оба плана подчёркивают важность тестирования новых сценариев.**

#### Промпт 2: Проверка кода

**Код для проверки: 
```bash

package ru.mentee.power.devtools.progress;
public class ProgressTracker {

    public String calculateTotalProgress(Mentee[] mentees) {
        if (mentees == null) {
            throw new IllegalArgumentException("Массив mentees не может быть null");
        }
        
        if (mentees.length == 0) {
            return "Нет данных о студентах";
        }
        
        int totalCompleted = 0;
        int totalTotal = 0;
        int index = 0;
        
        while (index < mentees.length) {
            Mentee current = mentees[index];
            totalCompleted += current.completedLessons();
            totalTotal += current.totalLessons();
            index++;
        }
        
        int totalRemaining = totalTotal - totalCompleted;
        return "Суммарно: пройдено " + totalCompleted + " из " + totalTotal + 
               " уроков, осталось " + totalRemaining + " уроков";
    }
}
 ``` 
 

**Что проверить:**
1. Логические ошибки
2. Соответствие Code Conventions
3. Производительность

**AI-1 ([DeepSeek]): 

<
1. Логические ошибки:

✅ Edge cases обработаны правильно: null проверка и пустой массив
⚠️ Возможный NPE внутри массива: Если элемент массива mentees[i] равен null, вызов current.completedLessons()
 выбросит NullPointerException (строка 25)
✅ Алгоритм корректен: Суммирование значений работает правильно
⚠️ Переполнение переменных: Для очень больших групп студентов возможен overflow totalCompleted и totalTotal (int ограничен ~2.1 млрд)

Рекомендации:
Добавить проверку на null элементы массива: if (current == null) continue; или выбрасывать исключение
Рассмотреть использование long для переменных если ожидается работа с большими числами

2. Code Conventions:
✅ Именование: Имена переменных соответствуют camelCase, понятные
⚠️ Длина метода: Метод выполняет несколько задач (валидация, подсчёт, форматирование)
✅ Комментарии: Javadoc присутствует, но можно добавить больше
⚠️ Магические числа: Строки сообщений хардкодены (лучше вынести в константы)
✅ Отступы и форматирование: Правильные

Рекомендации:
Разделить метод на несколько: validateInput(), calculateProgress(), formatResult()
Вынести строки в константы: private static final String NO_DATA_MSG = "Нет данных о студентах";
Добавить final к параметру: public String calculateTotalProgress(final Mentee[] mentees)

3. Производительность:
✅ Сложность алгоритма: O(n) — оптимально для этой задачи
⚠️ Индексация: Использование while с индексом вместо for-each немного менее читаемо
✅ Память: Не создаёт лишних объектов

⚠️ Конкатенация строк: В конце метода используется конкатенация, что создаёт несколько промежуточных String объектов

Рекомендации:
Использовать цикл for-each: for (Mentee mentee : mentees) { ... } — более читаемо и безопасно
Использовать String.format() или StringBuilder для финальной строки: 
return String.format("Суммарно: пройдено %d из %d уроков, осталось %d уроков", totalCompleted, totalTotal, totalRemaining);

Итоговые предложения по улучшению:
Добавить проверку null элементов массива
Заменить while на for-each цикл
Использовать String.format() для построения результата
Вынести строковые константы
Рассмотреть разделение ответственности метода**
>

**AI-2 ([ChatGPT]):

<
 1️⃣ Логические ошибки и edge cases
✅ Что сделано правильно

Проверка mentees == null (строки ~11–13)
✔ Корректно предотвращает NullPointerException
✔ Сообщение понятное, ошибка использования метода выявляется сразу

Обработка пустого массива (строки ~15–17)
✔ Пустой массив трактуется как валидный кейс
✔ Метод не заходит в while, логика проста и прозрачна
Корректный подсчёт через while
✔ Индекс увеличивается
✔ Нет риска бесконечного цикла
✔ Алгоритм корректен для непустого массива
⚠ Потенциальные логические проблемы
❗ Возможный NullPointerException внутри массива

Проблемное место:
Mentee current = mentees[index];
totalCompleted += current.completedLessons();

Проблема:
Если массив mentees содержит null элемент, код упадёт с NullPointerException.

Рекомендация:
Явно определить контракт:
либо запретить null элементы и выбрасывать IllegalArgumentException
либо пропускать null mentee (если это допустимо по бизнес-логике)
Для учебного проекта чаще выбирают первый вариант — он проще и делает ошибку очевидной.

❗ Некорректные данные в Mentee

Если:
completedLessons() > totalLessons()
или одно из значений отрицательное
то итоговая строка может быть логически некорректной (например, «осталось -5 уроков»).

Рекомендация:
Минимум — задокументировать ожидания к Mentee.
Лучше — валидировать данные при подсчёте или гарантировать корректность на уровне класса Mentee.

2️⃣ Code Conventions и читаемость
✅ Плюсы
✔ Имена классов и методов соответствуют Java Code Conventions
✔ Метод короткий, легко читается
✔ Используются осмысленные имена переменных (totalCompleted, totalTotal)
✔ Javadoc присутствует и полезен

⚠ Что можно улучшить
❗ Название переменной totalTotal

Проблема:
totalTotal читается тяжеловато и выглядит неестественно.

Рекомендации:
Более читаемые варианты:
totalLessons
overallLessons
totalPlannedLessons

Это улучшит восприятие без изменения логики.

❗ Javadoc не отражает исключение
Проблемное место: Javadoc метода
Метод выбрасывает IllegalArgumentException, но это не указано в документации.

Рекомендация:
Добавить @throws IllegalArgumentException — это особенно важно для публичных методов.

❗ Строковые литералы захардкожены

Проблема:
Сообщения:
"Нет данных о студентах"
"Суммарно: пройдено ..."

Рекомендация (необязательно для учебного проекта):
вынести в private static final String
упростит поддержку и возможную локализацию
3️⃣ Производительность и оптимальность
✅ Текущая ситуация
Временная сложность: O(n)
Память: O(1)
✔ Это оптимально для данной задачи
✔ Один проход по массиву — лучше уже не будет

⚠ Возможные микро-улучшения (не критично)
🔹 Проверка на переполнение int
Если массив большой или значения уроков высокие — возможен int overflow.

Рекомендация:
Использовать long, если:
ожидается масштабирование
проект не строго учебный

🔹 Формирование строки
Строка собирается через +, что абсолютно нормально здесь
операция выполняется один раз
StringBuilder не нужен
✔ Никаких изменений не требуется
📌 Итоговый вердикт
Код корректный, читаемый и соответствует уровню junior+/middle-.

Основные рекомендации (по приоритету):
🔴 Обработать null элементы внутри массива mentees
🟡 Улучшить именование totalTotal
🟡 Задокументировать @throws IllegalArgumentException
🟢 (Опционально) подумать о валидации данных Mentee
Если хочешь, в следующем шаге можем:
разобрать, как лучше обработать null mentee — fail-fast или skip
или сделать эталонную версию метода под code review / экзамен** >

**Сравнение: Оба анализа качественные и дополняют друг друга. Ваш анализатор лучше справился с
семантикой и бизнес-логикой, дал более практичные, готовые к применению советы. Мой анализ был
более техническим, сфокусированным на производительности и архитектурной чистоте.
Для учебного проекта рекомендации вашего анализатора более уместны — они конкретны, 
реализуемы и не перегружают код излишней сложностью. Для production-кода стоило бы учесть оба набора
рекомендаций, особенно про переполнение и разделение ответственности. **


# DVT-9

## Результаты само-ревью DVT-9

### Найденные проблемы

#### 1. Отсутствие CI badge в README

**Файл:** README.md  
**Проблема:** Нет бейджа статуса сборки GitHub Actions  
**Почему важно:** CI badge показывает статус проекта сразу при открытии README  
**Исправление:** Добавить бейдж в начало README

#### 2. Потенциальный NullPointerException в StudentList

**Файл:** StudentList.java  
**Проблема:** Метод `getStudentByCity()` может вызвать NPE при null значениях  
**Почему важно:** Может вызвать падение приложения в production  
**Исправление:** Добавить проверку null перед сравнением

#### 3. Неоптимальный цикл в ProgressTracker

**Файл:** ProgressTracker.java  
**Проблема:** Двойное обращение к массиву и неиспользуемая переменная  
**Почему важно:** Ухудшает читаемость кода  
**Исправление:** Использовать enhanced for-loop

#### 4. Отсутствие проверки элементов массива на null

**Файл:** ProgressTracker.java  
**Проблема:** Метод `calculateTotalProgress()` не проверяет элементы массива на null  
**Почему важно:** Может вызвать NPE если в массиве есть null  
**Исправление:** Добавить проверку `if (mentee != null)`

#### 5. Несоответствие code style

**Файл:** Все Java файлы  
**Проблема:** Возможные нарушения Checkstyle правил  
**Почему важно:** Единый стиль кода улучшает читаемость  
**Исправление:** Запустить Checkstyle и исправить предупреждения

## Code Review Checklist

### Функциональность

- Код решает поставленную задачу полностью
-Обработаны граничные случаи (null, пустые данные, экстремальные значения)
- Обработка ошибок реализована корректно
- 
### Тесты

- Добавлены тесты для нового функционала
- Все тесты проходят локально (./gradlew test)
-Покрыты позитивные и негативные сценарии

### Читаемость и стиль

- Имена переменных, методов и классов отражают назначение
- Нет дублирования кода
-Checkstyle проходит без ошибок (./gradlew checkstyleMain)

### Документация

- README обновлён (если добавлена новая функциональность)
- Публичные методы имеют JavaDoc (если применимо)
-Примеры использования актуальны

Шаблон конструктивного комментария:

**Проблема:** [Конкретное описание проблемы с указанием строки]
**Почему это важно:** [Объяснение последствий]
**Предложение:** [Как исправить или альтернативный подход]
**Пример:**

## Примеры Code Review комментариев

### Хорошие комментарии (конструктивные)

**Пример 1:**

**Проблема:** Метод `calculateDiscount` (строка 45) имеет 3 вложенных if-else и 40 строк.
**Почему это важно:** Сложная логика плохо тестируется и тяжело поддерживается.
**Предложение:** Вынести каждое условие в отдельный метод (например, `isEligibleForBonusDiscount()`)
и использовать паттерн Strategy для разных типов скидок.

**Пример 2:**

**Проблема:** Тест `testProcessOrder` (строка 78) проверяет только успешный сценарий.
**Почему это важно:** Не проверена обработка ошибок при недостаточном балансе.
**Предложение:** Добавить тест `testProcessOrder_InsufficientBalance_ThrowsException()`
с использованием `assertThatThrownBy()`.

### Плохие комментарии (неконструктивные)

**Пример 1:**

Этот код ужасен, полностью переписать.

**Почему плохо:** Нет конкретики (что именно плохо), нет предложения (как исправить),
токсичный тон (демотивирует автора).

**Пример 2:**

Здесь лучше использовать Stream API.

**Почему плохо:** Нет объяснения почему лучше, нет примера как переписать,
неясно какую проблему это решает.

## DVT-8
**Создайте feature-ветку:**
- В терминале:

  ```bash
  git checkout -b feature/DVT-9-ci
  ```

2. **Внесите изменение** (например, добавьте комментарий в `README.md`):

   ```bash
   echo "# CI-пайплайн настроен" >> README.md
   git add README.md
   git commit -m "Update README with CI info"
   git push -u origin feature/DVT-9-ci
     ```

3. **Создайте Pull Request:**
   - Откройте GitHub → ваш репозиторий → вкладка **Pull requests** → **New pull request**.
   - Base: `master`, Compare: `feature/DVT-9-ci`.
   - Кликните **Create pull request**, введите название и описание, создайте PR.

4. **Проверьте статус-чеки:**
   - Внизу страницы PR появится секция **Checks** (или **Some checks haven't completed yet**).
   - Подождите, пока GitHub Actions завершит выполнение (обычно 1–3 минуты).
   - Если все проверки прошли, увидите зелёные галочки: `✅ build / build-and-test`.
   - Если проверки упали, увидите красные крестики: `❌ build / build-and-test` — кликните Details, чтобы посмотреть логи.

**Checkpoint 5 (PR-проверки работают):** в pull request отображается секция Checks с результатами CI (зелёный статус, если код корректный).

## DVT-7

## Кодстайл-гайд проекта devtools

Проект следует правилам Google Java Style Guide с адаптацией.
Автоматическая проверка: ./gradlew checkstyleMain

### 1. Именование методов: camelCase

До:    public void add_student(Student s) { }
После: public void addStudent(Student student) { }

Почему: Java Convention требует camelCase для методов.
Источник: https://google.github.io/styleguide/javaguide.html#s5.3-camel-case

### 2. Пробелы после if/for/while

До:    if(condition) {
После: if (condition) {

Почему: улучшает читаемость, отделяет ключевое слово от выражения.
Источник: Oracle Code Conventions — Whitespace

### 3. Длина строки: максимум 120 символов

До:    public List getStudentsFromSpecificCityWithVeryLongName...
После: public List getStudentsByCity(String city) {

Почему: длинные строки затрудняют чтение в редакторе и при code review.
Источник: https://google.github.io/styleguide/javaguide.html#s4.4-column-limit

### 4. Порядок импортов

До:    import java.util.List; import java.util.ArrayList; import java.io.File;
После: import java.io.File; import java.util.ArrayList; import java.util.List;

Почему: алфавитный порядок упрощает поиск импортов.
Источник: IntelliJ IDEA → Code → Optimize Imports

### 5. Фигурные скобки для if

До:    if (condition) doSomething();
После: if (condition) { doSomething(); }

Почему: скобки обязательны даже для однострочных блоков.
Источник: https://google.github.io/styleguide/javaguide.html#s4.1.1-braces-always-used


## DVT-3
### Правило веток: feature/DVT-X с примерами master и feature/DVT-3

### 1 - Основная ветка (master)

DVT-6

## Сценарий ручной проверки DVT-6

### Запуск приложения

1. Откройте Gradle Tool Window (View → Tool Windows → Gradle)
2. Выполните: devtools → Tasks → application → run
3. Ожидаемый вывод в Run Tool Window:
   Суммарно: пройдено 25 из 36 уроков, осталось 11 уроков

### Запуск тестов

1. Откройте Gradle Tool Window
2. Выполните: devtools → Tasks → verification → test
3. Ожидаемый вывод: BUILD SUCCESSFUL, все тесты зелёные

### Отладка через Debug

1. Установите breakpoint на строке цикла while в ProgressTracker.calculateProgress
2. Запустите Debug: кликните правой кнопкой на main → Debug 'ProgressTracker.main()'
3. Используйте Step Over (F8) для прохождения итераций
4. Проверьте Variables: counter, remainingHours должны изменяться корректно
5. Используйте Evaluate Expression (Alt+F8): вычислите remainingLessons * 2
6. Ожидаемый результат Evaluate: 14 (для completedLessons=5, totalLessons=12)

### Что делать при ошибках

- Если вывод некорректен: проверьте логику цикла через Debug
- Если тесты красные: откройте вывод теста, найдите AssertionError, скорректируйте метод
- Если Debug не останавливается: убедитесь, что breakpoint установлен (красный кружок)



DVT-3
## Правило веток: feature/DVT-X с примерами master и feature/DVT-3

1 - Основная ветка (master)
   считается «основной» — здесь хранится стабильная версия кода;
   изменения вливаются только через merge-реквесты (pull requests) после ревью;
   не допускается прямой коммит — все доработки ведутся в feature-ветках.

### 2 - Feature-ветки (feature/DVT-X)

   создаются для реализации отдельных задач (например, feature/DVT-3 для задачи с ID DVT-3);
   название ветки строго по шаблону: feature/DVT-<номер_задачи> (например, feature/DVT-3, feature/DVT-10);
   ветка создаётся от master:
   -bash
   -git checkout master
   -git checkout -b feature/DVT-3
   в ней ведётся разработка, коммиты делаются по мере прогресса;
   по завершении работы ветка сливается в master через merge request;
   после слияния ветка удаляется.

## DVT-2 

Проект DevTools - Система отслеживания прогресса менти
Проект для отслеживания прогресса студентов в спринтах с
использованием современных инструментов разработки Java.

## Quick Start

Запуск через Gradle Tool Window (IntelliJ IDEA)
Откройте Gradle Tool Window (правая панель IDEA).

Разверните ветку Tasks.

Выполните:

build → двойной клик для сборки проекта.

run → запуск основного приложения.

test → запуск всех тестов.

Запуск через Run Anything (быстрый способ)
Нажмите Ctrl + Ctrl (дважды Ctrl).

Введите нужную задачу:

gradle run — запуск приложения.

gradle build — сборка проекта.

gradle test — запуск тестов.

Нажмите Enter для выполнения.

Альтернативно: командная строка
bash
./gradlew run    # запуск приложения
./gradlew build  # сборка проекта
./gradlew test   # запуск тестов

📦 Пакет ru.mentee.power
Пакет используется для:

Логической организации кода по доменной области

Предотвращения конфликтов имен классов через namespaces

Соответствия Java-соглашениям (обратный домен компании/проекта)

Управления видимостью классов через модификаторы доступа

📊 Таблица переменных/полей

menteeName - String	 - Имя менти, участвующего в спринте -  "Иван Петров"
sprintNumber - int - Номер текущего спринта в программе	- 3
plannedHoursPerWeek	- int-	Запланированное количество часов работы в неделю - 15 

https://mentee-power.xl.ru/learn/MCIneBj4KkyH-GIRCspFvA/theory

